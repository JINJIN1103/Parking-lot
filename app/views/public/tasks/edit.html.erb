<div class="ml-5">
  <h2 class="mb-5">タスク編集</h2>
  <div>
    <%= form_with(model:[@board, @list, @task], local: true) do |f| %>
    <div class="form-group">
      <label for="タスク名前">タスク名前</label>
        <%= f.text_field :title_t %>
    </div>

    <div class="form-group">
      <label for="タスク説明">タスク説明</label>
        <%= f.text_area :introduction_t ,:size=>"100x3" %>
    </div>

    <%= f.submit "変更を保存", class: "btn btn-success" %></p>
    <% end %>
  </div>
</div>



